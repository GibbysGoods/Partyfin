import { createTheme } from "@mui/material";
export const light = createTheme({ palette: { mode: "light" }, shape: { borderRadius: 16 } });
export const dark = createTheme({ palette: { mode: "dark" }, shape: { borderRadius: 16 } });
